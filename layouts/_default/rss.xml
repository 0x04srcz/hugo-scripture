{{- /*
  Section and taxonomy feeds.

  Hugo's built-in RSS template titles a feed from `.Title`, which for a
  section with no _index file is the pluralised directory name -- so
  `content/journal/` produced "Journals on <site>" while the section's own
  page heading said "Journal Entries". This uses the same
  helpers/section-title.html as the rest of the theme, so a section is named
  identically in its heading, its feed title, and the rel=alternate link
  advertising that feed.

  The home page feed is layouts/index.rss.xml, which is deliberately
  articles-only; this template does not affect it.
*/ -}}
{{- $authorEmail := "" }}
{{- $authorName := "" }}
{{- with site.Params.author }}
    {{- if reflect.IsMap . }}
        {{- with .email }}{{ $authorEmail = . }}{{ end }}
        {{- with .name }}{{ $authorName = . }}{{ end }}
    {{- else }}
        {{- $authorName = . }}
    {{- end }}
{{- end }}

{{- /* Only section pages get the helper. A taxonomy term page is also a node,
       but its .Title is the term itself ("OCaml"), while .Section is the
       taxonomy ("tags") -- so routing those through the helper would title
       every tag feed "Tags on <site>". */ -}}
{{- $title := .Title }}
{{- if .IsSection }}
    {{- $title = partial "helpers/section-title.html" . }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
    {{- $pages = $pctx.RegularPages }}
{{- else }}
    {{- $pages = $pctx.Pages }}
{{- end }}
{{- $limit := .Site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
    {{- $pages = $pages | first $limit }}
{{- end }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq $title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with $title }}{{ . }} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne $title .Site.Title }}{{ with $title }}in {{ . }} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>
    <language>{{ site.Language.LanguageCode }}</language>
    {{- with $authorEmail }}
    <managingEditor>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>
    <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>
    {{- end }}
    {{- with .Site.Copyright }}
    <copyright>{{ . }}</copyright>
    {{- end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- $isShortform := partial "helpers/shortform.html" . }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
      <guid>{{ .Permalink }}</guid>
      {{- /* Notes are short and meant to be read in full, so a summary would
             just be the note with its ending cut off. Send the whole thing. */}}
      <description>{{ if $isShortform }}{{ .Content | transform.XMLEscape | safeHTML }}{{ else }}{{ .Summary | transform.XMLEscape | safeHTML }}{{ end }}</description>
    </item>
    {{- end }}
  </channel>
</rss>
